# dépendances
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# données locales (créées à l'exécution)
# Les demandes, avis, galerie et photos vivent dans /data — jamais dans le dépôt.
/data/

# environnement
.env
.env.local
.env*.local

# divers
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

.site.pid
.tunnel.pid
site.log
tunnel.log
tunnel.conf
